{
  "name": "@daber/content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node --loader ts-node/esm src/build.ts",
    "report": "node --loader ts-node/esm src/report.ts",
    "clean": "rimraf dist && mkdir -p dist"
  },
  "dependencies": {
    "yaml": "^2.4.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5"
  }
}
