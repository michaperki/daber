# apps/web

**Placeholder.** Scaffolded in Phase 0, Step 4 (`docs/PLAN.md`).

This will be a Vite + Preact + TypeScript SPA with four tabs: Calibrate, Recognize, Practice, Vocab. The recognizer logic is pure TS; UI is Preact components; state lives in `@preact/signals`.

See:
- `docs/ARCHITECTURE.md` — directory layout and module boundaries
- `docs/PLAN.md` — Steps 4–11 for the build sequence
- `reference/hebrewhandwritingweb/app.js` — the logic being ported
